Integers in the JSON Object which are passed as empty strings are changed into -1, as the data provided by the ECDC can not be negative values. A trigger (SQL Statement can be found in ~/SQL_Code) changes the negative values to a SQL 'NULL' after insertion. 
